# v12: prepared, NOT sent

State on 2026-09-03. v11 went out on 2026-08-31 and has had no human reply;
the only responses are Sashiko's nine, adjudicated in `SASHIKO-V11.md`. v12
is prepared here so that it can go the day there is something to answer, and
it is held until then: a v12 three days after v11 with nobody having spoken
answers nobody.

## What v12 is

`v11-prep` in `~/Desktop/linux-next-v8` (next-20260814 + the fourteen) with
three commits amended, on branch `v12-prep`:

| patch | change | source |
|---|---|---|
| 3/14 | `pm_runtime_get_if_active()` tested for zero, reference put only when taken | Sashiko finding 2, `sashiko-fixes.diff` |
| 11/14 | NULL check on `of_device_get_match_data()`, probe fails `-ENODEV` | Sashiko finding 1, `sashiko-fixes.diff` |
| 14/14 | both cores and both IOMMUs enabled on the ROCK 4D, message reworded | the `a48679045` dts change from the `v11-attach` tree |

`git range-diff master v11-prep v12-prep` shows exactly those three. checkpatch
is clean on all three. Every mail scanned for invisible Unicode and for
non-ASCII: none.

The patches in this directory are regenerated by `send-v12.sh` (REGEN=1 is
the default), the cover comes from `cover-blurb.txt` through `splice-cover.py`.

## What still has to happen before it goes

1. **The board**: `sashiko-3-suspend.sh` answers whether the domain cycles at
   all under a real workload, which is what the open finding on 4/14 rests on.
   The cover carries a `*** BOARD RESULT HERE ***` line where the answer goes;
   `send-v12.sh` refuses to send while any `***` marker is in the cover.
2. **A reason to send**: a reply on v11, or a rebase forced by the tree moving.
3. **A decision on 3/14's Tested-by**: the cover keeps Igor's tag through a
   one-condition change and says so; drop it if that is the wrong call.

## The follow-up series: `../rfc-send-attach/`

The two rocket patches from `attach-once/` (keep the IOMMU domain attached
across jobs; handle the completion in the hardirq) on top of `v12-prep`, as
branch `attach-followup`. They do NOT apply to plain linux-next: both depend
on this series' restructuring of the job and reset paths, so they cannot be a
standalone series and are not folded into v12 either, which has waited
through eleven versions and should stay reviewable. They follow it.

Held until:
- the board says whether the core-pair overlap corruption was this kernel's
  per-job attach/detach or the runtime (the `board_intermittent.sh` recipe in
  charsiu); if it was the kernel, patch 1 fixes a corruption and the cover
  should say so with the numbers;
- Igor has run them on RK3588, which is the hardware the driver ships for.

What they are worth on the ROCK 4D, measured 2026-09-02 with charsiu:
decode +4% on four models, the per-call floor down, and the per-job
`iommu_group` reference leak Sashiko flagged on every v11 mail gone.
