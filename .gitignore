/dirty
/buildroot/br-out/
/buildroot/br-src/
/mesa/mesa-src/
/mesa/out/
/.claude/
/.venv/
init.md
build.md
rootfs-overlay/opt/npu-test/*.tflite
rootfs-overlay/usr/lib/python3.11/
# vendor-capture: track the scripts/docs/small references; skip the heavy deps,
# the local venv, build artifacts and large model/vendor blobs.
vendor-capture/rkt2-venv/
vendor-capture/__pycache__/
vendor-capture/work/
vendor-capture/*.whl
vendor-capture/librknnrt.so
vendor-capture/runner
vendor-capture/mobilenet_v1_1.0_224_quant.tflite
kernel/out/
vendor-capture/check*.onnx

# replay build artifacts (aarch64 ELF) + captured binary payloads
replay/replay
replay/*.so
replay/payload/
payload/
replay/replay_rocket

# generated rknn-decode artifacts (regenerable from gen_id_*.py)
vendor-capture/idg_*.rknn
vendor-capture/idb_*.rknn
vendor-capture/*.wt
