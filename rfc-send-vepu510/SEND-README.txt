[RFC PATCH 0/3] media: rockchip: VEPU510 H.264 encoder for RK3576
================================================================

Prepared but NOT sent. Review, then run ./send.sh (it confirms before
sending and prompts for your SMTP password). From: Jiaxing Hu
<gahing@gahingwoo.com>.

Files
-----
  0000-cover-letter.patch   the writeup: what works + the P-frame open question
  0001-...-rk3576-vepu.patch dt-bindings (yaml)
  0002-...-h264-encoder.patch the driver (README.md deliberately excluded)
  0003-...-vepu-nodes.patch   the RK3576 DT nodes

Recipients (send.sh)
--------------------
  To: mchehab (media), heiko (Rockchip), robh/krzk+dt/conor+dt (DT),
      nicolas.dufresne (Collabora - Rockchip codec, best bet for the
      inter-frame stall question)
  Cc: ezequiel (rkvdec), linux-media, devicetree, linux-rockchip,
      linux-arm-kernel, linux-kernel

Before sending
--------------
  - The patches are generated by hand from the linux-next work tree, not
    git format-patch, so the base isn't recorded. They are all new files
    plus small Kconfig/Makefile/dtsi hunks, so they should apply on a
    recent linux-next; if you want, apply them onto a clean linux-next
    checkout with `git am 000*.patch` first and rebuild to confirm.
  - git send-email must be configured (sendemail.smtpserver / user in
    your ~/.gitconfig, same as the NPU v2 send).

To send:
  ./send.sh
