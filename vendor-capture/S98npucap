#!/bin/sh
# Boot-time auto-capture of the vendor rknn PC-submit + regcmd sequence.
case "$1" in
	start)
		sleep 2
		/opt/npu-cap/run-capture.sh > /dev/console 2>&1
		;;
	stop) : ;;
	*) echo "Usage: $0 {start|stop}"; exit 1 ;;
esac
exit 0
