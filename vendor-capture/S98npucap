#!/bin/sh
# Boot-time auto-run of the VENDOR two-submit control (2026-07-16).
case "$1" in
	start)
		sleep 2
		/opt/npu-cap/run-twosubmit.sh > /dev/console 2>&1
		;;
	stop) : ;;
	*) echo "Usage: $0 {start|stop}"; exit 1 ;;
esac
exit 0
