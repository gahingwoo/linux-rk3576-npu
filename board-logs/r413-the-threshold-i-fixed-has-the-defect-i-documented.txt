round 413, 2026-09-15 — the threshold I corrected was measured against the
                        arm my own next section made faster

r412 found that 448 came from a sweep whose CPU column was not the CPU arm,
corrected it to 320, and then, 200 lines later in the same round, landed an in
place KV ladder that made the NPU arm faster at every length the arm is on.
Nobody re-asked the threshold. r412 §10 is a whole section about a refusal that
had to be re-asked "the moment the thing it refuses gets faster", and the fix
written beside it has the same defect.

  board     ROCK 4D, boot 0c9923fe (the board was off overnight and came back
            on a fresh boot; /opt/vendor and debugfs re-mounted by hand)
  npu clk   594 MHz, governor performance
  binary    /opt/charsiu/charsiu_run = 58d2d360d971, md5 d4b3b3d660e2, a CLEAN
            commit. Its --version answers, which is how this round knows.
  model     four, on the vendor's protocol: 128 token prompt, 64 new tokens

=====================================================================
1. ⛔⛔⛔ 320 HAS THE DEFECT 448 HAD, AND I SHIPPED IT KNOWING THE RULE
---------------------------------------------------------------------
r412's header says it in as many words:

    binary  /root/charsiu_run_win, md5 28c64b1a6a27 (= /opt/charsiu/charsiu_run,
            the r411 shipping binary; nothing was rebuilt for this section)

§2's table -- 252, 302, 352, 452, the table that produced 320 -- ran on that
binary. `charsiu_run_room`, the in place build, first appears in §7, 200 lines
later. So 320 was derived against the ALLOCATING arm.

The in place ladder only makes the NPU arm faster, and only where the arm is
on, so the crossover moves DOWN. 320 is too high by however much the ladder
buys at those lengths, and the arithmetic in §8/§19 of that round puts the
saving at 302 tokens near 62 ms, which would take that row from +2.3% against
a 2.2% spread -- level -- to roughly +5%, which clears.

⛔ 1) THE RULE WAS WRITTEN DOWN IN THE SAME FILE, TWICE. `src/llama.c` says
"If this arm gets faster again, re-measure. Do not scale this number." r412 §10
says a justification of the form "nothing measured says otherwise" names its
own expiry. Both were written before the ladder landed, and neither fired.

🔑 2) WHAT MAKES IT INVISIBLE IS THAT THE TWO HALVES WERE RIGHT SEPARATELY.
§2 correctly re-measured a threshold against a correctly named arm. §7
correctly made the arm faster. Nothing in either is wrong. The defect is
only in the ORDER, and a round that lands a speedup after a threshold has to
re-run the threshold or say it did not.

=====================================================================
2. ⚠⚠ AND THE TASK TERM IS NOT SMALL, IT IS NOT THERE
---------------------------------------------------------------------
The runtime fits `us a call = A + B a task + C a MB` on every staged run and
prints it. Two models, one boot, one binary, this round:

    Qwen3-0.6B      us a call = 68 + -0.1 a task + 118.4 a MB
    (second model)  us a call = 32 + 22.6 a task + 114.7 a MB

⚠ 3) B READS -0.1 AND 22.6. That is not a noisy estimate of one quantity, it
is a coefficient the data cannot determine: tasks and megabytes move together
across the shapes a real model presents, so the fit splits their sum almost
arbitrarily between them. The C term, which IS determined, reads 118.4 and
114.7 against a shipped 110.0 and is fine.

⛔ 4) SO `DEAL_US_TASK 36.8` IS WORSE THAN "FIVE TO TWELVE TIMES TOO LARGE",
which is how it was described last night. It is a number taken from a fit that
never determined it, and it is still steering which core every slice lands on.
The board list already has it. What this round adds is that re-deriving it from
the same kind of fit will not work either: the instrument that would price it
is the one that cannot see it.

⚠ 5) AND THE RUNTIME SAYS SO ITSELF, on the Qwen3 run: "that remainder is
NEGATIVE, so the hardware path and the wall clock are counting different calls
-- the rate above is not a fact about the hardware". The guard fired. Nobody
had read it against the fit on the line below.

=====================================================================
3. 🔑 THE IOMMU NUMBER: BOTH INSTRUMENTS NEED A FLASH, AND THE SAVING
   MAY NOT EXIST TO BE MEASURED
---------------------------------------------------------------------
"About 19.5 ms of a decode token is the per job IOMMU attach and detach" is
the one number in this project's writing that is derived rather than measured:
a per call floor multiplied by a call count. Two ways to settle it were
prepared, and preparing them answered a different question first.

⛔ 6) ftrace CANNOT RUN ON THIS BOARD. The kernel the board boots is snapshotted
in kernel/base.config, and it says

    # CONFIG_FTRACE is not set
    # CONFIG_KPROBES is not set

Everything in kernel/trace/Kconfig is inside `if FTRACE`, so there is no
function_graph tracer; fs/Makefile builds tracefs only under CONFIG_TRACING,
so /sys/kernel/debug/tracing does not exist there at all. There is no kprobe
route either. The symbols are all traceable in principle -- iommu_attach_group
and iommu_detach_group are exported globals with no notrace, and
rocket_job_run has its address taken by .run_job so it cannot be inlined -- and
none of that matters without the tracer.

So the "no patch needed" option does not exist. Getting this number requires a
reflash whichever instrument is chosen, which makes the debug patch the plan
rather than the fallback. It is prepared, off by default behind its own
Kconfig, and each of its five sites is a jump label nop when disarmed.

⭐⭐ 7) AND THE STRONG A/B MAY HAVE NOTHING TO WIN. An IOMMU domain belongs to
an open DRM FILE. charsiu_npu_open_mode calls charsiu_open(NULL) twice --
npudev.c:1561 and :1569 -- and open_any_accel answers both with the same
/dev/accel/accel0. Two files, two domains, and rocket_job_open gives each file
ONE scheduler entity spanning ALL cores, so either domain can land on either
core.

"Keep the domain attached across jobs" then only saves the jobs whose domain
did not change from the previous one. If the scheduler alternates the two
entities on a core, attach-once degenerates into detach plus attach on nearly
every job -- the baseline plus a kref dance, a LOSS.

🔑 8) SO THE CHEAP MEASUREMENT COMES FIRST, AND IT IS A CEILING. The debug
patch now also counts, per core, how often a job arrives carrying a different
domain than the one before it. One pointer compare. That percentage is the
hard ceiling on anything attach-once can remove, and it is readable from the
same run that gives the floor. Near zero and the A/B is not worth two boots,
and the 19.5 ms was never merely unmeasured: it was a saving that was not
available.

⚠ 9) TWO MORE THINGS THE A/B AS DRAFTED GETS WRONG. attach-once/0001 also
fixes a per job iommu_group_get leak, so its two arms differ by more than the
knob under test. And it is two kernels and two boots, against this tree's own
rule about never A/B-ing across boots on a board that drifts 3%. Gating the
fast path on a runtime knob instead makes it one kernel and one boot, which is
what every other board script here does.

=====================================================================
4. 🏁 THE ROUND OF RECORD, AND IT IS VALID
---------------------------------------------------------------------
Four models, seven repeats each, ONE boot, performance governor, the vendor's
own protocol (128 token prompt, 64 new tokens). tests/board_vendor.sh with
CHARSIU_BENCH_PERF=1 CHARSIU_BENCH_REPEAT=7.

🔑 THE BOOT ID IS THE SAME AT BOTH ENDS: 0c9923fe at the start and 0c9923fe at
the finish. That was this round's first red flag and it did not fire. The
previous attempt at this round died when the board reset mid-run.

    model              ours     theirs    margin   own spread   verdict
    Qwen3 0.6B        25.64      24.85     +3.2%        0.7%    ours
    TinyLLAMA 1.1B    21.96      19.71    +11.4%        2.3%    ours
    Phi3 3.8B          6.68       6.58     +1.5%        0.3%    ours
    Gemma4 E2B         8.91       9.23     -3.5%        0.6%    THEIRS

Leading with the one that lost: gemma-4 is 3.5% behind and that margin clears
its own 0.6% spread, so it is a real loss and not noise. The other three are
ahead by margins that clear their spreads too.

⚠⚠ 10) AND THE RIGHT COLUMN IS A CITATION, NOT AN ARM. RKNPU is not bound on
this boot -- only rocket is -- so the vendor runtime could not run and the
right column is their published benchmark.md table. It has no N and no spread,
so nothing in it can be "level within the noise": it has not got any. Their
figures are also taken at MAXIMUM CPU AND NPU frequency and this board's NPU
is at 594 MHz, which is the condition, not a property of either runtime.

⚠ 11) TTFT IS THEIRS ON ALL FOUR, 1.38x to 1.72x, AND THAT IS THE EXPECTED
SHAPE. This protocol is a 110 to 116 token prompt, which is far below the
lengths where the prefill work of the last ten rounds pays; the ladder in the
README is the instrument for prefill and it runs to 852. Quoting this TTFT
column as the prefill story would be quoting the one prompt length chosen to
suit their benchmark.

⭐ 12) EVERY MODEL'S FIRST READING IS COLD, AND BY A LOT. ttft 1084 against a
647 median on Qwen3, 1181 against 885, 3097 against 2896, 3014 against 2096.
The script reports the MEDIAN, which absorbs it; a best-of-N or a mean would
not have. The tree has had this rule since the day a 38% win turned out to be
3%, and this round is what it looks like when the instrument already knows.

=====================================================================
5. 🏁 THE NOISE FLOOR, MEASURED AT LAST: 2.5% WITHIN ONE BOOT
---------------------------------------------------------------------
Every "ahead" and "level" in this project is read against a 2.2% cross boot
drift bound, and r412 §6 found the same arm reading 2274 and 2138 at 302
tokens WITHIN one boot, 6.4% apart, unexplained. tests/board_drift.sh was
written for that and had never run.

One arm (CHARSIU_ATTN_NPU=0), one length (302 tokens), one boot, twenty
readings back to back with nothing changed, clock and governor pinned:

    median 2130   range 2110..2164   spread 2.5% of the median

🔑 13) SO THE FLOOR ON A WITHIN BOOT COMPARISON IS 2.5%, and the 2.2% bound
the documents quote is the right order of magnitude but slightly too small.
Nothing published changes sign: 302 is +0.7%, 602 +2.1% and 852 +1.2%, all
still level against 2.5%, and 452 was already level on its own 4.6% spread.

⭐ 14) AND r411's 2274 IS NOT DRIFT. Twenty readings span 2110 to 2164. 2274
is 5.1% above the largest of them, so it is not a sample from this
distribution at all. Whatever produced it was not the board wandering, and
r412 §6 was right to record it as unexplained rather than absorb it into a
noise bound.

⚠ 15) THE CPU FREQUENCY IS THE EVIDENCE, NOT THE TEMPERATURE. All twenty
readings sat at 2016000/2208000 kHz, so nothing throttled across a run long
enough to warm the board. The temperature column is BLANK and that is a gap
in the probe, not a reading of zero: this board publishes
/sys/class/thermal/cooling_device* and no thermal_zone*, so the glob matched
nothing. A probe that prints an empty column where a number belongs is the
shape this tree keeps being bitten by, and the frequency column is what
carries the argument here.

=====================================================================
6. 🏁 THE LADDER, RE-MEASURED, AND THE NULL CONTROL HOLDS
---------------------------------------------------------------------
tests/board_ttft_curve.sh on the shipping binary, five repeats a point, same
boot as everything above.

⚠ FIRST, A CORRECTION TO THE PLAN THIS ROUND WROTE FOR ITSELF. The plan said
"rows 27 through 302 are the null control". That was written before section 1
moved the threshold from 320 to 272, and 302 is above 272, so its arm goes
from OFF to ON and it MUST move. The control is 27, 52, 102 and 202. A
control set is defined by the thing under test, and the thing under test moved
underneath it in the same round -- the same shape as section 1 itself.

    tokens   r411    r413    change    arm
        27    332     331     -0.3%    off both sides
        52    415     409     -1.4%    off both sides
       102    761     750     -1.4%    off both sides
       202   1400    1395     -0.4%    off both sides
       302   2131    2045     -4.0%    OFF -> ON
       452   3059    2991     -2.2%    on both sides
       602   4115    4060     -1.3%    on both sides
       852   5953    5905     -0.8%    on both sides

🏁 16) THE FOUR CONTROL ROWS MOVED -0.3 TO -1.4%, ALL INSIDE THE 2.5% FLOOR
section 5 measured. Below the threshold the two arms run the same code -- there
is no mirror, no V surface, no ladder -- so they could not legitimately move,
and they did not. The re-read stands on that.

⚠ 17) AND THE THREE ROWS WHERE THE LADDER SHOULD HAVE HELPED ARE INSIDE THE
FLOOR TOO: -2.2, -1.3, -0.8%. That is not a contradiction of r412 section 8,
which measured 852 going 5937 -> 5859 with non-overlapping ranges. That was a
PAIRED comparison, two binaries alternating in one boot; this is two rounds on
two boots. A paired A/B resolves what a cross-round comparison cannot, and
quoting this table as evidence the in place ladder did nothing would be
comparing the wrong two things.

=====================================================================
7. ⭐ AGAINST THE VENDOR: AHEAD TO 602, LEVEL AT 852
---------------------------------------------------------------------
Their column is r393's measured ladder with their runtime on this board, a
different boot; their chat template costs a constant 33 tokens at every length
so each row is the same input text. A margin has to clear the 2.5% floor AND
the row's own spread.

    tokens     ours     theirs   margin   spread   verdict
        27      331      413.2   +24.8%     0.6%   ahead
        52      409      529.6   +29.5%     2.2%   ahead
       102      750      931.8   +24.2%     2.9%   ahead
       202     1395     1528.5    +9.6%     0.9%   ahead
       302     2045     2145.0    +4.9%     1.0%   ahead
       452     2991     3194.7    +6.8%     1.3%   ahead
       602     4060     4200.5    +3.5%     0.4%   ahead
       852     5905     6026.8    +2.1%     0.7%   LEVEL

🏁 18) SO THE PUBLISHED CLAIM MOVES: "level from 302 up" becomes "ahead from 27
through 602, level at 852". 302 and 452 were level before and clear both tests
now.

⛔ 19) AND 602 IS THE FRAGILE ROW, WHICH IS THE PART TO SAY OUT LOUD. +3.5%
clears the 2.5% floor by one point. A slightly stricter bound puts it back to
level. 302 (+4.9%) and 452 (+6.8%) clear comfortably; 852 (+2.1%) does not
clear at all.

⚠ 20) THIS IS THE TABLE THIS PROJECT ALREADY OVERCLAIMED ONCE. Every point
estimate favoured us then too, and it was written up as "completely surpassed"
and withdrawn the same day. The difference now is that the floor is MEASURED
(twenty readings, section 5) rather than quoted, and that one row is still
level and one is thin. Neither of those is a detail to leave out.
